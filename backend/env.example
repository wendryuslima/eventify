# ===========================================
# CONFIGURAÇÕES DE AMBIENTE - EVENTIFY BACKEND
# ===========================================

# Ambiente de execução (development, production, test)
NODE_ENV=production

# ===========================================
# BANCO DE DADOS
# ===========================================
# URL de conexão com PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database?schema=public
DATABASE_URL=postgresql://username:password@host:port/database?schema=public

# ===========================================
# SERVIDOR
# ===========================================
# Porta onde o servidor irá rodar
PORT=3001

# ===========================================
# CORS E FRONTEND
# ===========================================
# URL do frontend para configuração de CORS
# Para produção, use a URL do seu domínio
FRONTEND_URL=http://localhost:3000

# ===========================================
# LOGS E DEBUG
# ===========================================
# Nível de log (error, warn, info, debug)
# error: apenas erros
# warn: erros e warnings
# info: erros, warnings e informações
# debug: todos os logs incluindo queries SQL
LOG_LEVEL=info

# ===========================================
# SOCKET.IO (opcional)
# ===========================================
# Configurações específicas do Socket.IO se necessário
# SOCKET_CORS_ORIGIN=http://localhost:3000

# ===========================================
# SEGURANÇA (opcional)
# ===========================================
# Chave secreta para JWT ou outras operações de segurança
# SECRET_KEY=your-secret-key-here

# ===========================================
# RENDER.COM SPECIFIC
# ===========================================
# Para deploy no Render, essas variáveis são configuradas automaticamente:
# - NODE_ENV=production
# - PORT (definido pelo Render)
# - DATABASE_URL (se você conectar um banco PostgreSQL do Render)
